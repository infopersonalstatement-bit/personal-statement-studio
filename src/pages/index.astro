---
import BaseLayout from '../layouts/BaseLayout.astro';
import PageHero from '../components/common/PageHero.astro';
import ScrollReveal from '../components/common/ScrollReveal.astro';
import { PROTOCOLLI, BUNDLE_HUB } from '../lib/protocolli';
import { getPageSeo } from '../lib/page-seo';

const t    = Astro.locals.t;
const lang = Astro.locals.lang ?? 'it';

const pageSeo = await getPageSeo(Astro.locals.supabase, 'home', lang);

const scenarioCards = [
  { num: '01', title: t('home_sc_01_title'), desc: t('home_sc_01_desc') },
  { num: '02', title: t('home_sc_02_title'), desc: t('home_sc_02_desc') },
  { num: '03', title: t('home_sc_03_title'), desc: t('home_sc_03_desc') },
];

const { data: counts } = await Astro.locals.supabase
  .from('prodotti')
  .select('protocollo');

const glowCount    = (counts ?? []).filter((p) => p.protocollo === 'glow-up').length;
const digitalCount = (counts ?? []).filter((p) => p.protocollo === 'digital-identity').length;

const { count: bundleCount } = await Astro.locals.supabase
  .from('bundles')
  .select('*', { count: 'exact', head: true });
---

<BaseLayout
  title={pageSeo.seo_titolo ?? 'Home'}
  description={pageSeo.seo_descrizione ?? t('home_hero_subtitle')}
  ogImage={pageSeo.seo_og_image ?? undefined}
>

  <!-- ── HERO ──────────────────────────────────────────────────────────── -->
  <PageHero
    eyebrow={t('home_eyebrow')}
    subtitle={t('home_hero_subtitle')}
    subtitleWide={true}
    ctaText={t('home_cta')}
    ctaHref="/shop"
    titleSize="small"
    minHeight="100vh"
    navLinks={[
      { href: '/',          label: t('nav_home') },
      { href: '/manifesto', label: t('nav_manifesto') },
      { href: '/blog',      label: t('nav_blog') },
    ]}
  >
    {lang === 'it' ? (
      <>Crescita personale e identità digitale<br />
      per chi vuole <em class="not-italic font-semibold" style="color: var(--gold);">evolversi</em>.</>
    ) : lang === 'fr' ? (
      <>Croissance personnelle et identité digitale<br />
      pour ceux qui veulent <em class="not-italic font-semibold" style="color: var(--gold);">évoluer</em>.</>
    ) : lang === 'es' ? (
      <>Crecimiento personal e identidad digital<br />
      para quienes quieren <em class="not-italic font-semibold" style="color: var(--gold);">evolucionar</em>.</>
    ) : lang === 'de' ? (
      <>Persönliches Wachstum und digitale Identität<br />
      für alle, die sich <em class="not-italic font-semibold" style="color: var(--gold);">weiterentwickeln</em> wollen.</>
    ) : (
      <>Personal growth and digital identity<br />
      for those who want to <em class="not-italic font-semibold" style="color: var(--gold);">evolve</em>.</>
    )}
  </PageHero>

  <!-- ── MISSIONE ─────────────────────────────────────────────────────── -->
  <section class="py-24" style="background-color: var(--bg-surface);">
    <div class="mx-auto max-w-3xl px-6 text-center">
      <ScrollReveal>
        <p class="mb-4 text-xs font-semibold uppercase tracking-[0.3em]" style="color: var(--gold);">
          {t('home_mission')}
        </p>
        <p class="font-display text-2xl font-light leading-relaxed sm:text-3xl" style="color: var(--text);">
          {t('home_mission_h1')}
          <span class="block mt-3" style="color: var(--gold);">{t('home_mission_h2')}</span>
        </p>
      </ScrollReveal>
      <ScrollReveal delay={120}>
        <div class="mx-auto my-10 h-px w-24" style="background-color: var(--gold);"></div>
      </ScrollReveal>
      <ScrollReveal delay={220}>
        <div class="space-y-5 text-left text-base leading-relaxed sm:text-center" style="color: var(--text-muted);">
          <p set:html={t('home_mission_p1')} />
          <p>{t('home_mission_p2')}</p>
        </div>
      </ScrollReveal>
    </div>
  </section>

  <!-- ── QUALE PERCORSO ─────────────────────────────────────────────────── -->
  <section class="py-24 px-6">
    <div class="mx-auto max-w-6xl">
      <ScrollReveal>
        <p class="mb-4 text-center text-xs font-semibold uppercase tracking-[0.3em]" style="color: var(--gold);">
          {t('home_path_eyebrow')}
        </p>
        <h2 class="font-display mb-4 text-center text-3xl font-light sm:text-4xl" style="color: var(--text);">
          {t('home_path_h2')}<br />
          <em class="not-italic font-semibold" style="color: var(--gold);">{t('home_path_h2em')}</em>
        </h2>
        <p class="mx-auto mb-16 max-w-2xl text-center text-sm leading-relaxed" style="color: var(--text-muted);">
          {t('home_path_sub')}
        </p>
      </ScrollReveal>

      <div class="grid gap-8 sm:grid-cols-3">
        {scenarioCards.map((item, i) => (
          <ScrollReveal delay={i * 140} class="h-full">
            <div
              class="group h-full rounded-2xl border p-8 transition-all duration-300"
              style="border-color: var(--border); background-color: var(--bg-card);"
              onmouseover="this.style.borderColor='var(--gold)'"
              onmouseout="this.style.borderColor='var(--border)'"
            >
              <p class="mb-4 font-display text-4xl font-light" style="color: var(--gold);">{item.num}</p>
              <h3 class="mb-3 text-sm font-semibold uppercase tracking-widest leading-snug" style="color: var(--text);">{item.title}</h3>
              <p class="text-sm leading-relaxed" style="color: var(--text-muted);">{item.desc}</p>
            </div>
          </ScrollReveal>
        ))}
      </div>
    </div>
  </section>

  <!-- ── PROTOCOLLI IN EVIDENZA ─────────────────────────────────────────── -->
  <section class="py-24 px-6" style="background-color: var(--bg-surface);">
    <div class="mx-auto max-w-6xl">
      <ScrollReveal>
        <p class="mb-4 text-center text-xs font-semibold uppercase tracking-[0.3em]" style="color: var(--gold);">
          {t('home_vault_eyebrow')}
        </p>
        <h2 class="font-display mb-4 text-center text-3xl font-light sm:text-4xl" style="color: var(--text);">
          {t('home_vault_h2')}<br />
          <em class="not-italic font-semibold" style="color: var(--gold);">{t('home_vault_h2em')}</em>
        </h2>
        <p class="mb-16 text-center text-sm max-w-2xl mx-auto leading-relaxed" style="color: var(--text-muted);">
          {t('home_vault_desc')}
        </p>
      </ScrollReveal>

      <div class="grid gap-6 sm:grid-cols-2 lg:grid-cols-3">
        {PROTOCOLLI.map((proto, i) => {
          const count = proto.slug === 'glow-up' ? glowCount : digitalCount;
          const tagline = proto.slug === 'glow-up' ? t('proto_gup_tagline') : t('proto_di_tagline');
          const desc    = proto.slug === 'glow-up' ? t('proto_gup_desc')    : t('proto_di_desc');
          return (
            <ScrollReveal delay={i * 140} class="h-full">
              <a
                href={proto.href}
                class="group flex h-full flex-col rounded-2xl border p-6 transition-all duration-300"
                style="border-color: var(--border); background-color: var(--bg-card);"
                onmouseover="this.style.borderColor='var(--gold)'"
                onmouseout="this.style.borderColor='var(--border)'"
              >
                <p class="mb-2 text-[10px] font-semibold uppercase tracking-[0.3em]" style="color: var(--gold);">{t('proto_label')}</p>
                <h3 class="font-display mb-2 text-2xl font-light" style="color: var(--text);">{proto.nome}</h3>
                <p class="mb-3 text-xs font-semibold uppercase tracking-widest" style="color: var(--text-muted);">{tagline}</p>
                <p class="mb-4 flex-1 text-xs leading-relaxed" style="color: var(--text-muted);">{desc}</p>
                <p class="text-xs" style="color: var(--text-muted);">
                  {count > 0 ? `${count} ${t('home_avail')}` : t('home_coming')}
                </p>
              </a>
            </ScrollReveal>
          );
        })}
        <ScrollReveal delay={280} class="h-full">
          <a
            href={BUNDLE_HUB.href}
            class="group flex h-full flex-col rounded-2xl border p-6 transition-all duration-300"
            style="border-color: var(--border); background-color: color-mix(in srgb, var(--gold) 4%, var(--bg-card));"
            onmouseover="this.style.borderColor='var(--gold)'"
            onmouseout="this.style.borderColor='var(--border)'"
          >
            <p class="mb-2 text-[10px] font-semibold uppercase tracking-[0.3em]" style="color: var(--gold);">{t('bundle_label')}</p>
            <h3 class="font-display mb-2 text-2xl font-light" style="color: var(--text);">{BUNDLE_HUB.nome}</h3>
            <p class="mb-3 text-xs font-semibold uppercase tracking-widest" style="color: var(--text-muted);">{t('bundle_tagline')}</p>
            <p class="mb-4 flex-1 text-xs leading-relaxed" style="color: var(--text-muted);">{t('bundle_desc')}</p>
            <p class="text-xs" style="color: var(--text-muted);">
              {(bundleCount ?? 0) > 0 ? `${bundleCount} ${t('home_bundles_active')}` : t('home_no_bundles')}
            </p>
          </a>
        </ScrollReveal>
      </div>

      <ScrollReveal delay={120} class="mt-12 text-center">
        <a
          href="/shop"
          class="inline-block rounded-full border px-8 py-3 text-xs font-semibold uppercase tracking-widest transition-all"
          style="border-color: var(--gold); color: var(--gold);"
          onmouseover="this.style.backgroundColor='var(--gold)'; this.style.color='#0F0D0A'"
          onmouseout="this.style.backgroundColor='transparent'; this.style.color='var(--gold)'"
        >
          {t('home_cta')} →
        </a>
      </ScrollReveal>
    </div>
  </section>

  <!-- ── CTA FINALE ─────────────────────────────────────────────────────── -->
  <section class="py-32 px-6 text-center">
    <div class="mx-auto max-w-2xl">
      <ScrollReveal>
        <p class="font-display mb-6 text-4xl font-light leading-tight sm:text-5xl" style="color: var(--text);">
          {t('home_final_h2')}<br />
          <em class="not-italic font-semibold" style="color: var(--gold);">{t('home_final_h2em')}</em>
        </p>
      </ScrollReveal>
      <ScrollReveal delay={120}>
        <p class="mb-10 text-base leading-relaxed" style="color: var(--text-muted);">
          {t('home_final_sub')}
        </p>
      </ScrollReveal>
      <ScrollReveal delay={240}>
        <div class="flex flex-wrap items-center justify-center gap-4">
          <a
            href="/shop"
            class="inline-block rounded-full px-10 py-4 text-sm font-semibold uppercase tracking-widest transition-all"
            style="background-color: var(--gold); color: #0F0D0A;"
            onmouseover="this.style.backgroundColor='var(--gold-hover)'"
            onmouseout="this.style.backgroundColor='var(--gold)'"
          >
            {t('home_cta')}
          </a>
          <a
            href="/manifesto"
            class="inline-block rounded-full border px-8 py-4 text-sm font-semibold uppercase tracking-widest transition-all"
            style="border-color: var(--gold); color: var(--gold);"
            onmouseover="this.style.backgroundColor='var(--gold)'; this.style.color='#0F0D0A'"
            onmouseout="this.style.backgroundColor='transparent'; this.style.color='var(--gold)'"
          >
            {t('home_cta_manifesto')}
          </a>
        </div>
      </ScrollReveal>
    </div>
  </section>

</BaseLayout>
